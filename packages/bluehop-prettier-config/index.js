/**
 * Shared Prettier configuration.
 *
 * @type {import('prettier').Config}
 */
const config = {
  singleQuote: true,
  semi: false,
  tabWidth: 2,
  useTabs: false,
  printWidth: 100,
  trailingComma: 'all',
  arrowParens: 'always',
  bracketSpacing: true,
  endOfLine: 'lf',
}

export default config
